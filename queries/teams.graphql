query GetTeams($after: String, $first: Int = 50) {
  teams(after: $after, first: $first) {
    nodes {
      name
      id
      color
    }
    pageInfo {
      endCursor
      hasNextPage
    }
  }
}
