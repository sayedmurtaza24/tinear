query GetAllUsers($after: String, $first: Int = 50) {
  users(after: $after, first: $first) {
    nodes {
      id
      email
      name
      displayName
      isMe
    }
    pageInfo {
      hasNextPage
      endCursor
    }
  }
}
