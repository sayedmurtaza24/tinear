query GetStates($after: String, $first: Int = 50) {
  workflowStates(after: $after, first: $first) {
    nodes {
      color
      id
      name
      team {
        id
      }
    }
    pageInfo {
      endCursor
      hasNextPage
    }
  }
}
