query GetProjects($after: String, $first: Int = 50) {
  projects(after: $after, first: $first) {
    nodes {
      name
      id
      color
    }
    pageInfo {
      endCursor
      hasNextPage
    }
  }
}
